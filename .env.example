# Frontend → Worker. Leave empty to hide account sync (local-only app).
# Local: run `npm run worker:dev` and keep this URL.
# Production: the deployed Worker URL, e.g. https://rapidread-api.<subdomain>.workers.dev
VITE_API_URL=http://127.0.0.1:8787
